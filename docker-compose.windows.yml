#
# docker-compose.windows.yml — Windows Docker Desktop compatible
#
# Differences from docker-compose.yml:
#   - Removes `network_mode: host` (not supported on Docker Desktop for Windows)
#   - Uses explicit port mappings instead
#   - Uses Windows-style volume path for ~/.ulak
#
# Usage:
#   docker compose -f docker-compose.windows.yml up -d
#
services:
  gateway:
    image: erkanozdemir-labs/ulak-agent:latest
    container_name: ulak
    restart: unless-stopped
    volumes:
      - ${USERPROFILE}/.ulak:/opt/data
    environment:
      - ULAK_UID=10000
      - ULAK_GID=10000
    command: ["gateway", "run"]

  dashboard:
    image: erkanozdemir-labs/ulak-agent:latest
    container_name: ulak-dashboard
    restart: unless-stopped
    depends_on:
      - gateway
    volumes:
      - ${USERPROFILE}/.ulak:/opt/data
    environment:
      - ULAK_UID=10000
      - ULAK_GID=10000
      - ULAK_DASHBOARD_HOST=0.0.0.0
    ports:
      - "127.0.0.1:9119:9119"
    command: ["dashboard", "--host", "0.0.0.0", "--port", "9119", "--no-open", "--insecure"]
