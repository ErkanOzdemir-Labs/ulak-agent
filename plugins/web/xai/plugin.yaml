name: web-xai
version: 1.0.0
description: "xAI Web Search — search the web via Grok's agentic web_search tool (Responses API). Requires xAI Grok OAuth (via `ulak auth`) or XAI_API_KEY (https://x.ai)."
author: ErkanOzdemir-Labs
kind: backend
provides_web_providers:
  - xai
